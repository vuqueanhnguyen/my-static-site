# My Static Site (Scaffold)

This is a minimal static HTML site scaffold.

Quick start

1. Open this folder in VS Code.
2. Open `index.html` in the editor and right-click → *Open with Live Server* (install the Live Server extension), or simply open `index.html` in your browser.
3. Use the navigation bar to move between `index.html`, `about.html`, and `contact.html`.

Files

- `index.html` — main page with Home / About / Contact sections
- `styles.css` — basic styling
- `script.js` — small JS utilities (nav toggle, smooth scroll, demo form)

Next steps

- Edit content in `index.html` to add more pages or sections.
- Add a simple build/deploy setup (GitHub Pages, Netlify, or Vercel).
- Replace alert form handler with a backend endpoint or form service.
